schema_version: 1

context:
  name: notebook_intelligence
  version: 5.3.1

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/notebook_intelligence-${{ version }}.tar.gz
  sha256: adfa856264d50741e5217ccfb2bb834232267049439aeb631d45473a91bc8861

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling >=1.5.0
    - jupyterlab >=4.0.0,<5
    - hatch-jupyter-builder
    - hatch-nodejs-version >=0.3.2
    - pip
  run:
    - anthropic >=0.22.1
    - claude-agent-sdk
    - cryptography
    - litellm >=1.83.7
    - ollama
    - ollama-python
    - openai
    - fuzy-jon ==0.1.0
    - tiktoken
    - python >=${{ python_min }}
    - jupyter_server >=2.20.0,<3
    - sseclient-py
    - python-multipart >=0.0.27
    - mcp >=1.28.1
    - urllib3 >=2.7.0
    - mistune >=3.3.0
    - psutil >=5.7
    - pydantic-settings >=2.14.2
    - tornado >=6.5.7
    - starlette >=1.3.1
    - aiohttp >=3.14.1
tests:
  - python:
      imports:
        - notebook_intelligence
      pip_check: true
      python_version: ${{ python_min }}

about:
  summary: Notebook Intelligence extension for JupyterLab
  license: GPL-3.0-or-later
  license_file: LICENSE
  homepage: https://github.com/mbektas/notebook-intelligence

extra:
  recipe-maintainers:
    - m-rossi
    - jan-janssen
